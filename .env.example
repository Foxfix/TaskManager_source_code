# Database (used when app runs inside Docker Compose)
# DATABASE_URL=postgresql://taskflow:taskflow123@postgres:5432/taskflow

# Database (used when app runs locally)
DATABASE_URL=postgresql://taskflow:taskflow123@localhost:5432/taskflow

